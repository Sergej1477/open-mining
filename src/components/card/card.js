import './card.scss';

// const cards = document.querySelectorAll('.card');

// const onChangeCard = (evt) => {
//   const currentCard = evt.target;

//   cards.forEach((el) => {
//     el.classList.remove('card--active');
//     return currentCard;
//   });

//   currentCard.classList.add('card--active');
// };

// cards.forEach((el) => {
//   el.addEventListener('click', onChangeCard);
// });
